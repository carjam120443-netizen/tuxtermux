# APK compatibility

This directory is reserved for APK/Alpine Linux package-manager compatibility in TuxTermux.

## Status

🚧 Planned.

The intended design is to support an isolated Alpine Linux userspace where `apk` can operate without interfering with TuxTermux's native `apt`/`pkg` environment.
