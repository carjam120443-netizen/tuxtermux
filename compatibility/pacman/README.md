# Pacman compatibility

This directory is reserved for pacman/Arch Linux package-manager compatibility in TuxTermux.

## Status

🚧 Planned.

The intended design is to keep pacman-based environments isolated from TuxTermux's native `apt`/`pkg` environment while providing a convenient way to work with compatible Arch Linux userspaces.
